# =============================================================================
# 필수 API 키들
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# =============================================================================
# 데이터베이스 설정
# =============================================================================
# Docker 내부 연결 (litellm-proxy에서 사용)
DATABASE_URL=postgresql://llmproxy:dbpassword9090@postgres:5432/litellm
# 외부 연결 (로컬에서 접속 시): postgresql://llmproxy:dbpassword9090@localhost:5433/litellm

# =============================================================================
# LiteLLM 설정
# =============================================================================
LITELLM_MASTER_KEY=your_master_key_here
LITELLM_DROP_PARAMS=true
LITELLM_VERBOSE=false
LITELLM_STORE_MODEL_IN_DB=true
LITELLM_STORE_PROMPTS_IN_SPEND_LOGS=true

# =============================================================================
# Agent 설정 (로컬에서 사용)
# =============================================================================
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=sk-1234

# =============================================================================
# 모델 선택 설정 (선택사항)
# =============================================================================
# 코드 생성 단계에서 사용할 모델 (기본값: gpt-4o)
# CODE_GENERATION_MODEL=gpt-4o

# VLM 검증 단계에서 사용할 모델 (기본값: gpt-4o)
# 옵션: gpt-4o, llava (로컬 LLaVA 서버 사용 시)
# VLM_VALIDATION_MODEL=gpt-4o

# =============================================================================
# OpenPipe 설정
# =============================================================================
OPENPIPE_API_KEY=your_openpipe_key_here